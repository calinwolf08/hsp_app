# Step 2: Constants

## Objective
Define constants for API endpoints, status values, and configuration.

## Files to Create
- `src/lib/features/lms/shared/constants.ts`

## Constants to Define
- API endpoints for all collections
- Status values (not-started, in-progress, completed)
- Enrollment sources (direct, bundle, learning-path)
- Access types (sequential, automatic)
- Activity types (scorm, video, document, survey)
- Default pagination limits
- Query parameter builders

## Test File
- `src/lib/features/lms/shared/constants.test.ts` (endpoint builders, query params)

## Dependencies
- Step 1 (types)
