# Step 4: Content API

## Objective
Implement API functions for content collections.

## Files to Create
- `src/lib/features/lms/shared/api/content-api.ts`

## Functions to Implement
- `getLearningPaths(filters)` - Fetch learning paths
- `getLearningPath(id, depth)` - Fetch single learning path
- `getBundles(filters)` - Fetch bundles
- `getBundle(id, depth)` - Fetch single bundle
- `getCourses(filters)` - Fetch courses
- `getCourse(id, depth)` - Fetch single course
- `getSection(id, depth)` - Fetch section
- `getActivity(id)` - Fetch activity

## Test File
- `src/lib/features/lms/shared/api/content-api.test.ts` (mocked API responses)

## Dependencies
- Step 3 (API client)
